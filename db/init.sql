CREATE TABLE fruit_shard0 (
    id VARCHAR(255) PRIMARY KEY,
    name VARCHAR(50)
);

CREATE TABLE fruit_shard1 (
    id VARCHAR(255) PRIMARY KEY,
    name VARCHAR(50)
);

CREATE TABLE fruit_shard2 (
    id VARCHAR(255) PRIMARY KEY,
    name VARCHAR(50)
);